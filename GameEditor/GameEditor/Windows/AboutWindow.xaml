<Window x:Class="GameEditor.About"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameEditor"
        mc:Ignorable="d"
        Title="About" Height="200" Width="400" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style x:Key="CenterAll" TargetType="{x:Type TextBlock}">
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel VerticalAlignment="Center">
            <TextBlock Grid.Column="0" Grid.Row="0" Style="{StaticResource CenterAll}">This is a simple Game Editor</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource CenterAll}">Written by Stuart Mathews</TextBlock>
        </StackPanel>
    </Grid>
</Window>
